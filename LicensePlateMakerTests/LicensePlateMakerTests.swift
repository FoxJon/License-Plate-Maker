import UIKit
import XCTest
import LicensePlateMaker

class LicensePlateMakerTests: XCTestCase {

  //let maker = Maker()

  //test number of letters in alphabet
//  func testNumberOfLettersInAlphabet() {
//  XCTAssert(maker.alphabet.count == 26, "alphabet does not have 26 letters")
//  }
  
  //test that total results = (numbers+letters) to the power of (number of slots)
  
  
}
